1. set user and email
	git config --global user.name "Name"
	git config --global user.email "Email"
2. create folder and initail
	mkdir abc
	git init
3. add file and commit
	git add "fileName"
	git commit -m "message for log"

